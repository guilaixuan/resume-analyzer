# ===== 智能简历分析系统 - Python 依赖 =====

# --- Web 框架 ---
fastapi>=0.110.0          # 异步 Web 框架，原生 Pydantic 支持
uvicorn[standard]>=0.29.0 # ASGI 服务器，运行 FastAPI 应用
python-multipart>=0.0.9   # 处理 multipart/form-data 文件上传

# --- PDF 解析 ---
pdfplumber>=0.11.0        # PDF 文本提取（主引擎，精度高）
PyMuPDF>=1.24.0           # PDF 文本提取（备用引擎，速度快）

# --- AI 接口 ---
openai>=1.30.0            # OpenAI 格式的 AI 模型调用（兼容 DeepSeek/通义千问）

# --- 缓存 ---
redis>=5.0.0              # Redis 缓存客户端（可选，自动降级为内存缓存）

# --- 工具 ---
pydantic>=2.0.0           # 数据模型与校验（FastAPI 内置）
